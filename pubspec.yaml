name: flutter_learn
description: "Flutter Learn — แอปเรียนรู้ Flutter แบบเข้าใจง่าย พร้อมเดโมให้ลองเล่นจริง"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
